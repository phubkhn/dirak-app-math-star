# UI Specification

Status: starter. Claude Code must expand this file while building the prototype.

The canonical screen behavior is defined by `APP_SPEC.md`, `UI_DESIGN_BRIEF.md` and `screen-flow.md`. For each DES screen, document layout regions, components, state transitions, input behavior, audio cue and responsive differences between phone and tablet.

Do not copy CSS values here when a named value already exists in `design-tokens.json`.
